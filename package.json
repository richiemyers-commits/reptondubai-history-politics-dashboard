{
  "name": "reptondubai-history-politics-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/audit-secrets.mjs && node scripts/build.mjs",
    "pages:build": "npm run build",
    "pages:dev": "npm run pages:build && wrangler pages dev dist",
    "pages:deploy": "npm run pages:build && wrangler pages deploy dist --project-name reptondubai-history-politics-dashboard",
    "deploy": "npm run pages:deploy",
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "preview": "npm run build && STATIC_ROOT=dist node server.mjs",
    "audit:secrets": "node scripts/audit-secrets.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
